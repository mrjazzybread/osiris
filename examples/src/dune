;; Do not include this directory as part of this Coq project.
(include_subdirs no)

;; Compile these OCaml source files.
(library
  (flags (:standard -bin-annot))
  (name CoqOsirisExamples)
)

(alias
  (name default)
  (deps coqosirisexamples.cma)
)
