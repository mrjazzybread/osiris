pages:
  script:
    - mkdir public
    - cp coq-osiris/tutorial/* public
  artifacts:
    paths:
      # The folder that contains the files to be exposed at the Page URL
      - public
  only:
    changes:
      - coq-osiris/tutorial/*